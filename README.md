# adb-connect-qr

A Python CLI tool for pairing Android 11+ wireless debugging using QR codes.

## Installation

```bash
pip install adb-connect-qr
```

## Usage

```bash
adb-connect-qr
```
Scan the QR code in Android > Developer Options > Wireless Debugging > Pair device with QR code.
